## Dev Notes - V046

- Build: V046; base on V045; add checkpoint checkboxes inside the branch editor with add/remove controls.
- Checkpoints: editor now shows rows with checkbox + text; save reads these rows; add button for new checkpoints; delete button to remove; wrap is scrollable with expand/collapse toggle.
- UI: list rendering unchanged in cards (checkbox + strike-through).
- No other changes.

