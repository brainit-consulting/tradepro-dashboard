## Dev Notes - V046

- Build: V046; base on V045; refactor single HTML file into modular assets.
- CSS: extracted the inline `<style>` block into `tradepro_dashboard_v046.css`.
- JS: extracted the inline `<script>` block into `tradepro_dashboard_v046.js` with the same logic (IIFE preserved).
- HTML: now links to the external CSS/JS and keeps the same markup/behavior.
- Index: points to V046 for the refactored build.

