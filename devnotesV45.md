## Dev Notes - V045

- Build: V045; base on V044; add checkboxes for branch checkpoints with toggle persistence.
- Checkpoints: strings now normalized to objects { text, done }; checkbox toggle updates state and marks dirty; branch editor keeps text list.
- UI: checkpoint list shows checkboxes and strike-through when done.
- No other changes.

